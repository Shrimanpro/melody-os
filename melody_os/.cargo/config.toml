[build]
target = "aarch64-unknown-none"

[target.aarch64-unknown-none]
# We removed the rustflags array that contained -nostartfiles
runner = "qemu-system-aarch64 -M raspi3b -serial stdio -display none -kernel"
